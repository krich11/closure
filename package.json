{
  "name": "closure",
  "version": "1.2.1",
  "description": "Tab-Shame Absolution Engine — a privacy-first Chromium extension",
  "main": "background.js",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i src/popup.src.css -o popup/popup.css --minify && npx @tailwindcss/cli -i src/digest.src.css -o digest/digest.css --minify && npx @tailwindcss/cli -i src/settings.src.css -o settings/settings.css --minify && npx @tailwindcss/cli -i src/onboarding.src.css -o onboarding/onboarding.css --minify",
    "version:sync": "node scripts/sync-version.js",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "test:report": "npx playwright show-report"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/krich11/closure.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/krich11/closure/issues"
  },
  "homepage": "https://github.com/krich11/closure#readme",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@tailwindcss/cli": "^4.1.18",
    "tailwindcss": "^4.1.18"
  }
}
